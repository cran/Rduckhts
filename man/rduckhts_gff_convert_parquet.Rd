% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert_parquet.R
\name{rduckhts_gff_convert_parquet}
\alias{rduckhts_gff_convert_parquet}
\title{Convert GFF3 reader output to Parquet with DuckHTS metadata}
\usage{
rduckhts_gff_convert_parquet(
  con,
  path,
  output,
  columns = NULL,
  region = NULL,
  index_path = NULL,
  header = NULL,
  header_names = NULL,
  auto_detect = NULL,
  column_types = NULL,
  attributes_map = FALSE,
  attributes_list = FALSE,
  attributes_pairs = FALSE,
  strict = FALSE,
  where = NULL,
  compression = "zstd",
  row_group_size = 100000L,
  partition_by = NULL,
  include_metadata = TRUE,
  header_text = NULL,
  metadata = NULL,
  metadata_json_file = NULL,
  write_format_version = "1",
  overwrite = FALSE
)
}
\arguments{
\item{con}{A DuckDB connection with DuckHTS loaded.}

\item{path}{Path or URI to the input GFF3 file.}

\item{output}{Path to the output Parquet file or partitioned directory.}

\item{columns}{Optional character vector of columns to include. Defaults to all columns.}

\item{region}{Optional genomic region string for indexed inputs.}

\item{index_path}{Optional explicit index path.}

\item{header}{Logical; pass `header := true/false` to `read_gff(...)`.}

\item{header_names}{Optional character vector of column names.}

\item{auto_detect}{Logical; request type auto-detection.}

\item{column_types}{Optional character vector of DuckDB column types.}

\item{attributes_map}{Logical; expose GFF attributes as `MAP(VARCHAR, VARCHAR)`.}

\item{attributes_list}{Logical; expose attributes as `MAP(VARCHAR, VARCHAR[])`.}

\item{attributes_pairs}{Logical; expose attributes as key/value/index structs.}

\item{strict}{Logical; enable strict GFF validation while scanning.}

\item{where}{Optional SQL predicate applied to the reader output before conversion.}

\item{compression}{Parquet compression, default `"zstd"`.}

\item{row_group_size}{Parquet row group size.}

\item{partition_by}{Optional character vector of output partition columns.}

\item{include_metadata}{Logical; include DuckHTS Parquet KV metadata.}

\item{header_text}{Optional corrected header text to store instead of the source header.}

\item{metadata}{Optional named list/vector of extra metadata. This is the
primary CRAN/offline-safe path for arbitrary metadata; values with the same
names as DuckHTS defaults override the default values.}

\item{metadata_json_file}{Optional path to a JSON file containing a top-level
object of extra metadata. This requires DuckDB's `json` extension to be
available when the conversion SQL is generated; otherwise DuckDB will report
its normal missing-extension error. Use `metadata` for offline-safe metadata.}

\item{write_format_version}{DuckHTS Parquet write-format version string.}

\item{overwrite}{Logical; replace an existing output path. The wrapper checks
existence through DuckDB `glob(...)` where possible and passes the same flag
to DuckDB `COPY` for partitioned-output overwrite handling.}
}
\value{
Invisibly returns `output`.
}
\description{
Thin DBI wrapper around extension macro `duckhts_gff_convert_parquet_sql(...)`.
}
